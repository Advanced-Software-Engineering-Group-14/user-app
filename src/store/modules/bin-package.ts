
import {
    createAsyncThunk,
    createEntityAdapter,
    createSlice,
    // Dictionary,
    EntityId,
} from "@reduxjs/toolkit";
  
import { BinPackageRes } from "../../types";

interface FactsState {
    
}